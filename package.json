{
  "name": "@aitrust/receipt-verify",
  "version": "0.1.0",
  "description": "JS SDK for verifying AI receipts (offline; salted hashes support)",
  "main": "src/verify.js",
  "bin": { "receipt-verify": "bin/receipt-verify" },
  "scripts": {
    "test": "node bin/receipt-verify examples/ok.json && node bin/receipt-verify examples/ok.json --salt 73616c74 --mode prefix"
  },
  "license": "Apache-2.0",
  "dependencies": { "ajv": "^8.17.1", "ajv-formats": "^3.0.1" }
}
